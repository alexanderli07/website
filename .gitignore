# The site is static and has no build step. node_modules only ever holds sharp,
# for regenerating the icons and the social card by hand — see scripts/.
node_modules
*.local
.vercel

# Left over on disk from the React app this replaced. Nothing writes it now;
# it stays ignored so a stale build can never be committed by accident.
dist
dist-ssr

# editor / OS
.DS_Store
Thumbs.db
.vscode/*
!.vscode/extensions.json
.idea
.claude/
